"""Sustainable Stock PDC committee driven by local agent CLIs.

The metered provider gateway remains untouched.  This package adds a parallel
committee that speaks through locally installed subscription CLIs, so a daily
run costs a fixed seat instead of per-token spend.
"""
